# Secrets / user data — NEVER commit
.gobbonet-secret
*.secret
*.key

# Models and engine (way over GitHub's 100MB limit)
models/
llama.cpp/
llama/
*.gguf
*.exe
*.dll
*.zip
*.7z

# Runtime junk
logs/
*.log
cache/
temp/
tmp/
conversations/
# Runtime state — never commit
.gobbonet-state.json
.swap-status.json
.last-lan-ip
active-model.json
.llama-launch.cmd
.embed-launch.cmd

# Generated at runtime by fileserver.ps1 (the port it actually bound).
.gobbonet-port
